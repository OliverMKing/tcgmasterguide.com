BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[comments] ALTER COLUMN [deck_slug] NVARCHAR(1000) NULL;
ALTER TABLE [dbo].[comments] ALTER COLUMN [deck_title] NVARCHAR(1000) NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
